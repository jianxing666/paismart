spring:
  datasource:
    url: jdbc:h2:mem:paismart;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  data:
    redis:
      host: localhost
      port: 6379
      password:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /

app:
  rabbitmq:
    queue:
      file-processing: file-processing-queue
      file-processing-dlq: file-processing-dlq
    exchange:
      file-processing: file-processing-exchange
      file-processing-dlx: file-processing-dlx
    routing-key:
      file-processing: file-processing

admin:
  bootstrap:
    enabled: false

knowledge:
  bootstrap:
    enabled: false

jwt:
  secret-key: bXlfc2VjdXJlX2tleV8xMjM0NTY3ODkwMTIzNDU2Nzg5MDEyMzQ1Njc=

minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: test-bucket
  publicUrl: http://localhost:9000

file:
  parsing:
    chunk-size: 512
    overlap-size: 100
    min-chunk-size: 100
    parent-chunk-size: 1048576
    buffer-size: 8192
    max-memory-threshold: 0.8
    pdf:
      engine: liteparse
    liteparse:
      command: lit
      ocr-enabled: false
      ocr-language: eng
      max-pages: 1000
      dpi: 150
      num-workers: 0
      timeout-seconds: 300

elasticsearch:
  host: localhost
  port: 9200
  scheme: http
  username: elastic
  password: changeme
  insecure-trust-all-certificates: true
  init:
    enabled: false

security:
  allowed-origins: http://localhost:3000

logging:
  level:
    org.springframework: WARN
    com.yizhaoqi.smartpai: INFO

deepseek:
  api:
    url: http://localhost:11434/v1
    model: deepseek-chat
    key:

embedding:
  api:
    url: http://localhost:8000
    key:
    model: text-embedding-v4
